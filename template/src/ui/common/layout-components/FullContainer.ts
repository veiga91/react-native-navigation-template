import styled from 'styled-components/native';
import { ViewProperties } from 'react-native';

const FullContainer = styled.View<ViewProperties>`
  flex: 1;
  height: 100%;
  width: 100%;
  background-color: red;
`;

export default FullContainer;
