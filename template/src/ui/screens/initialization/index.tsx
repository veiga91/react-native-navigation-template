import React from 'react';
import { FullContainer } from 'layout-components';

const AppPreLoad: React.FC<{ componentId: string }> = ({ componentId }) => {
  return (
    <FullContainer>
    </FullContainer>
  );
};

export default AppPreLoad;